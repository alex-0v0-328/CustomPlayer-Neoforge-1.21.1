package com.unknown.customplayer.custom.enums.body;

//  Which severity ladder a part is measured on; the two never mix. SENSE = three degrees (top two
//  stored, degree one is a MobEffect); BODY = four grades by how hard each is to undo. See vault.
public enum PartScale {
    SENSE,
    BODY,
}
