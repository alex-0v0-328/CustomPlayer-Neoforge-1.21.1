package com.unknown.customplayer.custom.enums.body;

/**
 * The two scales a part is graded on; the scale decides which ailments it can carry.
 *
 * @author Alex
 * @since 1.0.0
 */
public enum PartScale {
    SENSE,
    BODY,
}
